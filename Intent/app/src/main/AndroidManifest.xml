<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sg.edu.nus.intent">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Intent"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityIntent1" />
        <activity android:name=".ActivityIntent5" />
        <activity android:name=".ActivityIntent6" />
        <activity android:name=".ActivityIntent2" />
        <activity android:name=".ActivityIntent3" />
        <activity android:name=".ActivityIntent4"></activity>
    </application>

</manifest>